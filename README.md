# clairo

Sniff out performative `wgpu` code

`clairo` is a Rust benchmark library for `wgpu` command workloads. It uses an NVBench style measurement loop to collect GPU timings across repeated samples
